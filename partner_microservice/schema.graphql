# schema.graphql

type DayDiff {
  days: Int!
}

type AppDate {
  date: String!
}

type Query {
  getCardActivityDayDiff(date: String!): DayDiff
  getAppActivityDate: AppDate
  updateAppActivityDate: AppDate
}